# MCP承認全体是正・実証と受入記録

2026-09-17。設計工程。製品コード・設定・サービスは未変更。fixtureの合格、実カタログ取得、実ツール実行、Discordの表示・本人押下・結果を別々に記録する。

## D01 全定義の照合（実施済み）

Gatewayの隔離カタログ記録と全体監査を照合。4サーバー342件、トップレベル引数1,082件。定義ハッシュ・型・必須・公開候補／構造化検討／本文検討／秘密・コード／プロトコルの区分を台帳へ保存した。定義確認は342件の動作試験を意味しない。詳細投影の意味レビューは続行中。

## D02 カタログ取得方式の実証計画（実施前に記載）

目的：全Apps/MCPを残した実構成で、Thread指定・toolsAndAuthOnly・サーバー単位ページングを組み合わせた取得が成立するか確認する。

- 導入版Codex 0.153.4。既存の生成config/authをprivateな一時CODEX_HOMEへ複製する。内容や生カタログを公開ログへ出さない。
- initialize、thread/start、mcpServerStatus/listだけ使用。AI Turn・MCP tool call・承認返信・上流許可設定変更・Discord投稿・常駐再起動は行わない。
- 同一Threadでlimit=1、detail=toolsAndAuthOnly、threadIdを指定。cursorを使って最大64ページ。全体60秒、各RPC30秒、1ページ8MiB・全体32MiBを計測する。値は設計検証用であり、製品へ適用済みではない。
- 合格条件：全サーバーが取得され、既知3形式の定義と全342件との差異が説明できる。Thread指定時の欠落・失敗は隠さず記録。elapsed、件数、bytesだけ記録する。
- 終了時に隔離プロセス群を停止し、一時認証・状態を削除する。常駐のMCPやセッションを停止しない。

## 製品受入（未実施）

F01 全カタログ・全通常形式の初回完全表示。
F02 取得遅延／失敗／上限／復帰／並行／Thread分離。
F03 各ツールの正常引数・境界引数・秘密・未知キー・変更有無の投影。
F04 外部送信／共有変更／削除／コード／秘密／認証をターン許可しない。
F05 後続の引数変化にも毎回ポリシーを再適用する。初回の許可だけでは回避できない。
F06 Stop／Steer／次発言／別Run／別会話／設定変更／取消／期限／復元。
F07 Discordの完全表示・本人押下・結果と、未確定配信・古い表示の拒否。
F08 全target回帰・Clippy・fmt。実構成での性能／メモリ／リソース回収。

全体を満たす前に製品完成・本番配備可とはしない。

### D02の実測結果

- サンドボックス内：4ページ計4.42秒、319件。Serena runtimeStatus=failedで23件欠落。**不合格**。未対応rendererとは別のサーバー起動障害。
- 同じ隔離試験を承認済みの制限外実行で再試行：4ページ計30.47秒（12.83／6.01／5.51／6.12秒）、全342件。各ページ964,643／2,812／28,912／30,405 bytes。全件の定義はGateway保存済み342件と一致、追加・欠落なし。サンドボックスの影響と整合するが、起動失敗の全発生原因を断定しない。
- **Thread指定で全件取得可能という実証は成功。** limit=1では30秒を超えたため、仮の全体30秒deadlineを確定しない。HTTP GETでこの全処理を同期実行する設計も採用しない。
- 追加計画D03：同じ全構成・Thread指定・toolsAndAuthOnlyでlimit=100を測定し、余分なページRPCの影響を比較する。副作用範囲・終了手順はD02と同一。全342件の一致、応答bytes、elapsedを合格条件とする。

F09 初回カード導線：通常操作の試験で「本人限定／拒否」しか表示されない場合は不合格。通常3択と、外部送信等の単発2択を分け、実際の操作内容が初回に表示されることを本人が確認する。真のプライバシー例外は本人限定画面の内容＋許可選択まで確認する。

### D03の実測結果

Thread指定・toolsAndAuthOnly・limit=100で1ページ、10.30秒、1,026,691 bytes、全342件取得。旧10秒timeoutを超えた。D02より少ないRPCで全件取得できたが、単一回の計測を性能保証とは扱わない。外側GETへ同期連結する方式は採用しない。失敗を未対応と誤分類しない設計、非同期取得・共有・期限・起動状態の考慮が必要。

F09補完（利用者の説明確認後の決定）：プライバシー例外は「自分だけに表示して確認」→押した本人だけに見える内容＋許可・拒否でよい。別人への内容非開示、ボタンを開くだけでは実行しないことを検証する。通常操作をこの例外へ不当に落とさない。

### 0.5追加受入条件

F10 本人向け分割表示：全ページ同一版、任意ページ再取得、ページ欠落・重複・他人・他会話・別audience・版変更・ページ順違い・配信結果不明・再起動を検証する。全ページtokenが揃わなければProxyは許可を送らない。Gatewayは実際に表示できなかったページを表示済みとしない。公開カードは1ページに収まらなければ分割途中で許可させない。
F11 profile互換：旧Response、旧Gateway、新Gateway、未知profile、0.5で旧reply bodyを送る迂回を検証する。標準OpenAI互換APIへ変更を混入しない。

## D04 文書整合と公開範囲の期待値（設計検査）

公開範囲の規則と架空入力30例を[判定仕様](mcp-approval-privacy-design.ja.md)／[期待値JSON](mcp-approval/privacy-cases.json)へ追加。実装コードに対する30件のテスト成功という意味ではない。製品実装後に、通常入力の過剰な本人向け化と機密入力の公開漏れの両方を照合する。

分割表示は全ページの表示成功前に許可しないが、拒否は途中でも可能とした。Gatewayの保存値はProxyの鍵付き不透明fingerprintに限定し、生の機密本文の無鍵digestを保存しない。台帳の作用候補とAPIの作用enumを区別した。

D04の文書検査結果：JSON応答例4件の構文、関連文書の相対リンク、342件の定義ハッシュとツール名、1,082引数のキー、Markdown台帳342行、判定例30件のID・結果enumを照合し一致。`git diff --check`も成功。製品テストは未実施。最新台帳はT=950、C=42、R=30、P=20、S=4、H=10、O=26。Rの追加6引数にもツール全体のfixture・全引数設計は残っており、未完了24引数の意味評価だけが残作業の全てという意味ではない。

## GatewayレビューR-01／R-02の追加受入（未実施）

F12 照会と保存：0.5 operation／interaction.operation／presentation／grant一覧の全scopeと世代を照合。nullと省略、受理時履歴と現在の適格性を区別。grant_policyから許可範囲を復元でき、旧canonical_scopeへ新世代を落としても一致扱いにならない。Gateway再起動・Proxy再起動・取消・新旧Response混在・未知profileを試験する。0.3/0.4省略設定は依頼中許可なし、0.5省略設定は評価済み通常policy、明示空mapは全版で依頼中許可なし。

F13 正常更新：依頼中許可→30秒経過→同じ通常操作→同一定義で更新成功を、合成試験・実モデル・実Discordで別々に確認する。更新中は許可送信なし、更新後の適用1回、追加クリックなし、カード連投なし、元grantの期限延長なし。Gatewayが許可を自動送信していないことも確認する。

F14 更新待機の中断と競合：grant期限、interaction期限、call待機60秒、worker60秒、停止／Steer／取消／拒否／手動許可／定義変更／取得失敗を境界の前後で試験する。最終確定は一度だけ。call固有の期限切れを共有worker全体の障害にしない。catalog失敗後は同一定義に戻っても旧grantを復活させない。catalog設計C08〜C10に対応する。

R-01/R-02追記後の文書検査：API内のJSON例7件を構文検査し、operation／presentation／interaction.operation／grantのscope・世代・fingerprint・policyの対応を照合、関連文書のリンクと`git diff --check`も成功。製品コードの試験や実DiscordでF12〜F14が成功したという意味ではない。

## D05 接続合意とNotion複合操作の設計照合

Gatewayの0.5第11〜13節再レビューによりR-01/R-02は解消。接続仕様を合意済みへ更新した。内部詳細設計は続行中。

提供元のNotion fetchでview-dsl-spec／enhanced-markdown-specを読取。ページ・DBの私的データの取得や変更操作は行っていない。FORMによる権限変更、page/database記法による既存資源移動、同期元への変更、子リソース削除条件を[詳細設計](mcp-approval-notion-design.ja.md)と台帳に反映した。

設計期待値20件を導入カタログのJSON Schema（Draft 2020-12 validator）と照合。通常表示・本人向け表示を想定した全16例は入力schemaを満たす。否定例4件のうち空のold_str（NP08）はschemaで拒否されるが、未知のDSL命令（NV10）・未知の権限値（NV11）・未評価option（NP09）はschemaだけでは通る。そのため意味・作用の判定を独立して必須とする。これは期待するProxy判定器を実装して20件が通ったという意味ではない。

残るNotionの内部詳細設計は、属性型に依存する解釈の根拠取得／callへの拘束、全構文の解析規則、null等の仕様差、追加変更対象の表示。接続仕様の再レビュー待ちへ戻していない。既存の全342件のラベル・意味評価・fixture対応のゲートも維持する。

Gatewayの部分置換指摘を受け、Notion設計第3.1〜3.2節へ対象本文・置換意味・全適用状態・版の対応・更新競合の確認を追加。既存20例は対象根拠を要する条件付き期待値と明示した。追加NC01〜NC10はタグ属性・合成・コード囲み・同期元・全一致・本文欠落・競合・適用順不明・中間作用の設計期待値で、上流実行試験の結果ではない。

Gateway内部設計1.1再照合：GD-01〜03解消。許可JSON例はProxyのターン許可例からgrant_scopeを除いた単発bodyと完全一致、拒否例はrevisionとresponseのみであることを構文・値で照合。Gateway側の接続設計照合は完了。Proxy側詳細設計の未完了は維持し、製品コード・常駐変更は行っていない。

2026-09-17利用者決定：保証できないNotion部分置換は実行しない。現版update_contentのrelease_expectedはunavailable／単発・依頼中許可ともfalse。既存20例・文脈例10件は意味評価用の仮定と製品期待値を分離した。call対応・本文取得・grantが揃っていても未確認の実行時版保証を補完せず、旧grantや単発replyで迂回できない試験を追加する。


2026-09-17 内部設計継続：共通処理・DB移行・競合境界を[Proxy内部設計](mcp-approval-v05-proxy-internals.ja.md)へ集約。342操作名・1,082引数ラベルを付与したが、意味の照合済みとはしていない。利用者決定をNotion／表示設計／API／利用者説明へ反映し、NR-01〜07を追加。NP08・NP09は入力検証を優先してunknown_arguments、それ以外の現版update_contentはoperation_semantics_unverifiedとした。

今回の文書検査：設計JSON 5ファイルの構文、update_content 5例の製品期待値（全件許可不可、理由の優先順位）、関連文書の相対リンク、git diff --checkを確認。上流・Discordの実行試験ではない。承認通知を通らない常時許可経路の制御点確認、他の複合操作の意味・fixture照合は残っている。現行コードのnative adapterはitem/tool/requestUserInputを入口とするため、この入口だけを検査して全経路の実行禁止を保証したとは扱わない。


2026-09-17 実行制御の設計確認：Codex --version=0.153.4と、公式ソースtag rust-v0.153.4／3d2ee51ca2d5db578f328aa75e20aa22c0197c9aを照合。promptでは承認を要求し過去の許可キーを使わないこと、approve・never／権限条件・reviewerで迂回し得ること、Appsの正確なtool nameとmanaged設定優先を確認。[実行制御](mcp-approval-execution-boundary.ja.md)にEB-01〜07を追加。実Codexへ新設定を入れた承認到達試験は未実施。

GitHub GT01〜17とDrive DC01〜12の29件を追加。正常系17件は取得済み定義のJSON Schemaに適合。異常系12件のうちschemaで拒否するのはDC10の1件だけで、残る11件にはProxyの意味検証が必要。この結果を製品判定器の試験合格と混同しない。342定義ハッシュ・1,082引数キーが元カタログと一致、設計JSON・相対リンク・git diff --checkも確認。


2026-09-17 責務再整理：利用者は部分置換禁止を明示的にポリシー指定した実行だけへ適用すると決定。MFR-17〜21、層別の責務、単発承認と意味評価の独立、非選択実行への設定漏れ防止を追加し、LR-01〜07を定義した。既存のNotion release_expectedは当該ポリシー適用時の期待値として範囲を明記。全342意味解析完了を汎用承認の着手条件とする旧ゲートは改訂対象とした。後継wire・scope・runtime／Thread設定分離の詳細化と接続レビューは未完了。これを0.5の接続再合意済みと扱わない。文書・JSON・リンク検査のみ、製品コード・常駐変更なし。


2026-09-17 後継API具体化：API 0.6レビュー案、18区分のJSON例、V06-01〜10を追加。未選択／選択、未評価単発、公開／本人向け、禁止、返信、許可一覧を記述。scope・binding・policy世代・返信token・grantの一致、応答サイズ、JSON・相対リンク、git diff --checkを検査した。operationの長い引数を65536 bytes応答へ無理に詰めず、arguments_deliveryで全ページ取得と未取得を区別する。Gateway接続レビュー、runtime／Thread分離の実証、実装試験は未完了。


API 0.6 C06-01/02補完：第12節の応答表、登録／発行容量、エラー、第13節の60秒準備期限、停止・復旧・占有状態を追加。全execution_policy例にpreparation、capabilityにresponse_limitsと準備上限、準備照合／期限切れ／設定不明／隔離／停止の6組と停止要求・応答例を追加した。既存v2のoperation状態accepted/runningと取消code=execution_cancelledを照合。

[境界検査記録](mcp-approval/api-v06-boundary-validation.json)：単一項目は合法でもdescriptor全体3234 bytesで2048 bytesを超える例、合法サイズのdescriptor32件で追加capability66311 bytesとなり65536を超える例を確認した。grant16件＋外枠の予約上限794624 bytes、interaction256件＋外枠50339840 bytesを算術検証。JSONの65535/65536/65537 bytesも正確に構成して境界を確認。これは設計値と例の検査であり、製品の登録拒否・容量制御が動く試験ではない。JSON・scope/binding・policy期限不変・準備中占有・停止同一ID・相対リンク・git diff --checkを確認。Gateway再レビューは未了。
