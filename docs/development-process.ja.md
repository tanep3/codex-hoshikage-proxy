# 要件・設計を先行させる開発工程

2026-09-16。ユーザー指示に基づく工程上の必須条件。

## 原則と今回の是正

MCPターン許可では、契約書の補完とコーディングを並行して進めてしまった。文書の完成を実装開始条件として確認しなかったことが原因である。先行コードを正本にして仕様を追認せず、文書を先に完成させる。先行コードは未完了として保持し、開始条件が満たされるまで追加実装を停止する。配備は別の条件で判定する。

## 開始条件

| 工程 | 開始条件 | 完了時に残す証拠 |
| --- | --- | --- |
| 要件・設計 | 依頼と既存契約を確認 | 責務、要件、制約、未確定事項 |
| 必要な実証 | 文書へ実証目的・範囲・合格条件を記載 | 観測事実、限界、仕様への影響。製品実装と区別 |
| 具体契約完成 | API、全状態、競合、エラー、保持・上限、互換性、受入を記載 | 文書間の整合確認、レビュー指摘対応表、完成版パス |
| 実装 | 上記文書が完成・提示済み、接続上の指摘が解消済み | 基準とした契約版、要件と実装の対応 |
| 受入 | 基準版に沿う実装が完成 | 受入IDごとの結果と証拠、未検証事項 |
| 配備 | 受入条件を満たし、配備の承認範囲を確認 | 対象版と配備後確認 |

文書の「完成」、双方の「合意」、コードの「実装済み」、試験の「受入済み」、サービスの「反映済み」を別々に記録する。未検証事項は受入条件・合格基準が具体化されていれば文書に残せるが、API形式や責務の未決定を「後で実装しながら決める」として実装開始しない。

## 変更要求が来た場合

1. 影響するコーディングを止め、要件・設計・契約・受入条件へ先に差分を反映する。
2. 既存仕様との矛盾と対向システムへの影響を確認し、指摘ごとの回答を記録する。
3. 改訂版を提示し、対向接続上の指摘が解消してから実装を再開する。既存の合意や承認を無意味に取り直すことはしない。
4. 途中報告には現在の工程と未完了事項を明記する。文書を更新した事実だけで「仕様確定」と報告しない。

この手順の例外を担当者の判断で設けない。ユーザーが順序変更を明示した場合だけ、その指示と適用範囲を記録する。

## 今回の工程記録

- 要件・設計・API契約0.3：Proxy提示用の整合確認と文書完成。
- Gatewayレビュー：0.1への5指摘は0.3の対応表で回答。2026-09-16、Gatewayの照合記録で0.3受入・5指摘解消を確認。接続レビュー完了。
- 実装：開始条件を充足し、0.3を基準に再開可能。先行着手分は未完了であり、確定契約との適合確認が必要。コードを正本にしない。
- 実証：実browser_evaluateのID・引数対応付けのみ確認。操作をCancelで終了。反復自動許可・実Discord受入とは別。
- 常駐：今回の自動許可は未有効化。文書提示は配備承認ではない。

接続レビューの証跡：Gatewayリポジトリ `docs/mcp-turn-approval-api-review.ja.md` の「0.3の照合結果（2026-09-16）」。ユーザーから同内容の受入連絡を受領した。A01〜A11の受入や常駐自動許可の有効化は、この工程判定に含めない。

### 0.3基準の実装再開・試験（2026-09-16）

Gateway受入確認後、ユーザーから「双方合意ですので、進めてください」と実装再開の指示を受領。先行コードを0.3に照合し、競合・表示照合・上限処理を補完した。実施範囲と残る受入条件は[検証記録](mcp-turn-approval-validation.ja.md)に分離して記録する。常駐自動許可は未有効化。

### Proxy単独追加受入（2026-09-16）

追加受入の計画を先に検証記録へ記載し、障害・競合・実ブラウザー試験を実施した。許可TTLが壁時計後退で延長する問題は、システム設計書へ単調時計による期限境界を補完した後に修正した。検証記録へ結果と未確認範囲を追記。Gatewayの実装・常駐反映連絡を受領したが、Proxy常駐配備・自動許可有効化・実Discord受入の完了とは扱わない。

### 2026-09-16 20:31 JST：接続試験用の常駐反映

利用者の明示指示でProxyを常駐反映し、本機能をON、対象をplaywright.browser_findに設定した。上記の未反映・未有効化は配備前時点の記録。現在は結合試験可能で、実Discord受入は未完了。[配備記録](server-operations.md)を参照。
